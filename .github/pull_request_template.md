## Summary

- What changed?
- Why was this needed?

## Validation

- [ ] `docker compose up -d` starts successfully
- [ ] `GET /health` returns `ok`
- [ ] If WebUI changed: `agent-webui` builds (`npm run build`)
- [ ] Docs updated if user-facing behavior changed

## Risk

- Potential regressions:
- Rollback plan:
